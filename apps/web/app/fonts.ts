// SF Pro is Apple's system font — it cannot be bundled.
// On macOS/iOS, -apple-system / BlinkMacSystemFont resolves to SF Pro automatically.
// The className below is a no-op placeholder so layout.tsx compiles unchanged.
export const sfPro = { variable: "" };
